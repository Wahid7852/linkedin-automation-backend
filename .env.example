# Path to the persisted LinkedIn session cookies (created by `cli.py login`)
COOKIES_PATH=cookies.json

# Host/port for the REST server
HOST=0.0.0.0
PORT=8000

# --- Phase 2 (summary generation) ---
# ANTHROPIC_API_KEY=sk-ant-...
