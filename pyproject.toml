[project]
name = "linkedin-profile-backend"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "playwright>=1.44.0",
    "fastapi>=0.110.0",
    "uvicorn[standard]>=0.29.0",
    "python-dotenv>=1.0.0",
]

[tool.vercel]
entrypoint = "linkedin_backend.server:app"
